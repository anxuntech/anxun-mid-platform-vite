# P2 最终验收报告

## 验收状态

当前状态：本地实现与自动化验收完成，等待生产迁移、账号创建、部署及线上验证后封板。

## 已实现能力

- MySQL 正式账号、角色、项目绑定、服务器会话和认证审计。
- bcrypt 密码哈希、随机会话令牌、HttpOnly 安全 Cookie、CSRF 和会话撤销。
- 安巡管理员与项目查看者两类后端角色。
- 平乡正式页面、聚合、企业、记录、报告和下载接口统一后端保护。
- 正式数据固定读取 P1 MySQL，不允许使用 JSONL 或演示数据兜底。
- 公开 `/gov/pingxiang-demo` 固定使用演示数据且不请求真实 API。
- 阶段报告 PDF、企业运行汇总 Excel、四项业务汇总 Excel和受控企业记录明细。
- 登录成功与失败、退出、拒绝访问、敏感详情、账号管理和下载审计。
- 草料诊断接口仅管理员或内部密钥访问；原始事件摘要已裁剪。
- Webhook 共享密钥、频率限制、请求体限制和拒绝日志保护。

## 本地验证

- 全新依赖安装：通过。
- 服务端自动化：通过。
- TypeScript：通过。
- 生产构建：通过。
- 浏览器路由、演示隔离、状态页、桌面和移动端回归：通过。
- 依赖审计：已应用全部非破坏性补丁修复。完整依赖树剩余 4 项（中 3、高 1、致命 0），其中高风险位于 Vite 开发构建链；生产依赖树剩余 2 项中风险，均来自 React Router。生产服务器使用 `npm ci --omit=dev` 且线上只提供静态产物，不运行 Vite 开发服务；Vite 和 React Router 的完全修复均要求跨主版本，P2 不进行破坏性升级。当前返回地址已采用同源路径白名单，并明确拒绝协议相对地址和反斜杠，后续单独安排路由与构建工具升级。

## 生产验收待填写

- 部署提交：
- 回退提交或标签：
- RDS 备份标识：
- `005_p2_auth_foundation` 迁移结果：
- 三个正式账号联调：
- 下载和敏感访问审计：
- 账号禁用旧会话测试：
- P1 Webhook 四类回归：
- 官网、中台、平乡演示页回归：

## 最终结论

只有生产验收全部通过后，结论更新为“P2 已完成并通过生产验收”。在此之前，不将本地通过误报为线上正式可用。
