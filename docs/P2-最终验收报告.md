# P2 最终验收报告

## 验收结论

P2 已完成并通过生产验收。

正式平乡入口已采用后端账号、服务器会话、角色和项目范围控制；公开演示入口继续固定使用演示数据。当前 RDS 中平乡项目尚无 `real` 业务记录，正式页面因此如实显示空数据，不使用 `test` 或演示数字补齐。

## 代码与部署

- P1 基线提交：`031ef0d1e459e0a5522e45815f69ddedff709b76`
- P2 业务收口提交：`9401cd3`
- 功能分支：`codex/p2-auth-rbac-production`
- 生产分支：`main`
- Deploy Main：`30187813937`
- 部署结果：成功，构建、服务端回归、TypeScript、静态资源同步、服务端同步和 PM2 重载均通过
- P2 上线前回退标签：`p2-pre-auth-20260726`
- P1 完成标签：`p1-complete-20260726`

## 数据库与备份

- 上线前 RDS 手工完整备份：`3093423338`
- 备份任务：`133097600`
- 备份结果：成功
- `005_p2_auth_foundation`：线上执行成功
- 已创建：账号、项目授权、会话、认证审计和下载审计表
- 正式账号：安巡管理员、平乡应急局查看账号、保险机构查看账号，共 3 个，均为启用状态
- 正式初始密码未进入 Git、报告或普通日志，仅保存在服务器 root 专用交接文件中，权限为 `600`
- Node 线上数据库账号：`anxun_runtime`
- 平乡业务记录环境核对：`test` 88 条，`real` 0 条

## 认证与权限验收

- 未登录访问正式页面会进入登录页。
- 匿名访问正式聚合接口返回 `401`。
- 三类正式账号均可登录、恢复会话和退出。
- 密码使用 bcrypt 哈希保存，会话令牌只保存 SHA-256 摘要。
- 会话 Cookie 使用 `HttpOnly`、`Secure` 和 `SameSite=Lax`。
- 平乡应急局和保险账号仅能读取已授权项目。
- 草料诊断接口：管理员可访问，查看账号返回 `403`。
- 汇总报告和两类汇总表：三类账号均按授权可下载。
- 企业原始记录明细：仅管理员可下载，查看账号返回 `403`。
- 账号禁用后旧会话立即失效，账号恢复后需重新登录。
- 下载与认证操作均已形成服务器端审计记录。

## 正式与演示数据隔离

- 正式入口：`/gov/pingxiang`
- 公开演示入口：`/gov/pingxiang-demo`
- 正式入口固定读取 P1 MySQL 中 `real` 环境数据。
- 正式接口不允许回退到 JSONL、`test` 数据或前端演示数据。
- 演示入口不请求正式认证和真实数据接口。
- 正式页面已使用“真实环境”“当前账号授权范围内的项目数据”等明确口径。
- 正式数据缺失时显示“暂无数据”，不自动使用演示数字。

## Webhook 安全验收

- 草料回调已配置受控 Webhook 认证令牌。
- 无令牌和错误令牌请求保持快速应答，但不会写入 RDS 或进入业务处理。
- 正确令牌请求可进入既有业务链路。
- 受控测试确认数据库事件增量仅为 1 条。
- P1 四类识别、入库、去重和重放逻辑未被重写。
- Webhook 仍通过 PM2 进程 `anxun-caoliao-webhook` 运行。

## 本地自动化验收

- 服务端测试：24/24 通过。
- TypeScript：通过。
- 生产构建：通过，Vite 5.4.21，共转换 1648 个模块。
- 浏览器自动化：15/15 通过。
- 覆盖正式入口登录拦截、返回地址绕过、核心路由、指标下钻、趋势下钻、企业分页、四类业务详情、报告下载、空/少/长文本状态和移动端。
- 依赖审计已应用全部非破坏性修复。剩余问题需要 Vite 或 React Router 跨主版本升级，不在 P2 内进行破坏性升级。

## 线上验收

- 官网：`200`
- 中台：`200`
- 平乡公开演示页：`200`
- 平乡正式页面：`200`，未登录由前端进入登录流程
- 平乡正式聚合 API 匿名访问：`401`
- 正式页面生产资源：已核对为 `9401cd3` 构建资源
- 正式页面浏览器控制台：0 错误、0 警告
- 正式页面当前真实数据：0 家企业、0 条四类业务记录，如实展示空数据
- 报告 PDF：`200`，有效 PDF 文件
- PM2 进程：在线
- 生产环境文件：`600:root:root`
- Nginx P2 前备份：`/etc/nginx/config-backups/default.before-p2-auth-20260726-120848`
- 账号交接文件：`600:root:root`

## 回退演练

- 使用 P2 上线前 Nginx 备份临时替换当前配置并执行 `nginx -t`：通过。
- 演练期间未 reload，不影响线上请求。
- 演练退出后自动恢复当前配置，并再次执行 `nginx -t`：通过。
- 代码回退使用 `p2-pre-auth-20260726`。
- 数据库回退使用 RDS 备份 `3093423338`；只有在确认无须保留的会话和审计数据后，才允许单独执行 005 回退脚本。
- 回退不得让正式真实数据接口恢复为匿名访问。

## 已知但未扩大的技术债

- 当前尚无 `real` 企业和四类业务记录；这是数据接入状态，不是鉴权故障。后续真实数据必须由正式连接器和企业映射进入，不得将 `test` 数据改标为 `real`。
- GitHub Actions 提示 Node 20 运行时将由 Actions 强制切换到 Node 24；本轮未改部署工作流。
- Vite 与 React Router 的剩余依赖告警需要跨主版本专项升级。

## 最终结论

P2 已完成真实后端登录、角色与项目范围控制、正式接口保护、安全会话、下载分级、审计留痕和 Webhook 认证。正式页面已可安全向授权账号开放，但当前没有正式业务数据，因此保持真实空数据状态；公开平乡页面继续作为演示环境。
