# P3 生产验收报告

## 版本与部署

- P3 功能提交：`db38c40`
- P3 合并提交：`0918408`
- 首次完整成功部署：GitHub Actions `30194266818`
- 部署目标：`https://axsztech.cn`
- 数据助手数据环境：`test`
- 数据助手开放范围：仅安巡管理员

## 数据库迁移

- 迁移前确认 `ai_query_audit_logs` 不存在，`006_p3_ai_assistant` 未登记，不存在部分迁移。
- 迁移前备份：`/opt/backups/anxun-mid-platform-vite/anxun-platform-before-p3-006-20260726-161413.sql.gz`
- 备份已通过 gzip 完整性校验，文件权限为仅 root 可读。
- `006_p3_ai_assistant` 已成功执行并登记。
- 审计表共 19 个字段、4 个索引，外键关联账号与项目。
- 新建 `anxun_migrator` 专用迁移账号，仅授予当前业务库所需的结构迁移权限。
- `anxun_runtime` 继续使用原有运行权限，不授予建表、删表或账号管理权限。
- 迁移凭据保存在 `/root/.config/anxun/rds-migration.env`，权限为 `600 root:root`，未进入 Git。

## 权限与数据隔离

- 匿名访问平乡真实数据接口返回 `401`。
- 匿名访问数据助手接口返回 `401`。
- 政府项目查看账号访问数据助手返回 `403`。
- 安巡管理员可以访问数据助手，并且只能查询其已授权的平乡项目。
- 数据助手固定读取 `source_environment=test`，不读取 `real` 数据。
- 平乡和宁晋公开演示页面继续使用内部演示数据，不展示 RDS `test` 数据。

## DeepSeek 与审计

- 自然语言问题已真实调用 DeepSeek，未触发本地降级。
- “平乡县当前未闭环隐患有多少？”被识别为 `query_unclosed_hazards`。
- 固定查询返回 8 条，审计表记录结果数量、模型名、Token 使用量、状态和时间。
- DeepSeek 仅负责意图识别，不接收数据库连接、SQL、密码、密钥或完整业务明细。
- DeepSeek 密钥仅保存在服务器 root-only 环境文件中，源码和前端产物不包含密钥。

## 回归验证

- 官网返回 `200`。
- 平乡正式入口返回前端页面，真实数据接口保持登录保护。
- 平乡公开演示页返回 `200`。
- 宁晋公开演示页返回 `200`，显示“演示环境”且使用独立县域演示数据。
- 草料 Webhook 返回固定 `200` 与 `{"success":true,"message":"received"}`。
- PM2 进程 `anxun-caoliao-webhook` 在线并已加载 P3 配置。
- 管理员浏览器页面可见“项目数据助手”入口和侧边面板。
- 宁晋演示页浏览器检查无控制台错误。

## 结论

P3 的受控 AI 数据助手、多县域演示骨架、权限隔离、审计记录与生产部署链路已通过验收。当前仅使用受控 `test` 数据验证 AI 能力，未开放政府外部平台接口，也未将真实企业数据暴露给公共页面。
