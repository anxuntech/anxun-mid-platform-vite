# P1 最终验收报告

验收日期：2026-07-26

## 一、任务边界

- P1 负责 RDS MySQL 数据底座、Webhook 入库、迁移、历史时间修复、受控数据链路验证、运行账号最小权限、备份与回退能力。
- 登录鉴权完成后开放真实数据页面属于 P2，不计入 P1 缺口。
- 公开平乡页面继续使用演示数据，真实数据读取接口保持后端保护，不向公网匿名开放。

## 二、代码与部署

- P1 收口提交依次为：
  - `38be32c73b9dc4d6246282df3c0d675fe3bb6856`
  - `b0d72d2`
  - `8b29e0d`
  - `174238a4e1cae9cb0e2b28aa314f85df4c5c098c`
- 所有提交均通过普通快进推送进入 `main`，未强制推送。
- 代码回退标签：`p1-pre-close-20260726`，对应提交 `25fa4be84764031ec000d16745e3914857bfddc0`。
- GitHub Actions 部署记录：
  - `30182427639`：成功
  - `30182754494`：成功
  - `30183056572`：成功
  - `30183163771`：成功
- 本地门禁：
  - Server 测试：13/13 通过
  - TypeScript：通过
  - Vite 构建：通过
  - Playwright：12/12 通过
  - `git diff --check`：通过

## 三、备份与迁移

### RDS 完整备份

- 部署前完整快照：`3093266039`，状态 `Success`。
- 时间修复前完整快照：`3093286429`，状态 `Success`。
- 两次均为实例级 `FullBackup / Snapshot`。

### 服务器备份

- 目录：`/opt/backups/anxun-mid-platform-vite/p1-close-pre-20260726-085733`
- 包含：
  - `webhook-app.tar.gz`
  - `www-html.tar.gz`
  - `runtime-data.tar.gz`
  - `SHA256SUMS`
- 三个备份包均完成 SHA-256 校验。
- 备份文件权限已收紧为 `600`。

### 004 迁移

- `004_p1_data_contract` 已于北京时间 `2026-07-26 09:10:51.086` 在线执行成功。
- `schema_migrations` 当前包含 `001`、`002`、`003`、`004`。
- 部署迁移脚本已改为先只读确认迁移表是否存在，普通发布不再无条件要求 DDL 权限。
- 新增迁移仍需临时授予维护账号 DDL 权限，执行后应立即收回。

## 四、历史 8 小时时差

### 成因

历史实现把 UTC ISO 时间直接去掉时区后写入 MySQL `DATETIME`，而数据库生成的 `created_at` 使用北京时间，造成部分记录相差 480 分钟。该问题与本机 VPN 或日本时区无关。

### 识别与修复

- 只识别 `created_at - received_at` 在 475 至 485 分钟之间的确定错误记录。
- 候选清单：`/root/.config/anxun/audit/p1-timezone-candidates-20260726.json`，权限 `600`。
- 共识别 4 条：
  - 隐患 1 条
  - 巡检 3 条
- 修复前已创建完整快照 `3093286429`。
- 修复后 4 条记录的偏差均为 0 分钟。
- 再次执行 dry-run，候选数为 0，证明脚本幂等。
- 未对其余 204 条记录统一加 8 小时。

## 五、四类受控数据链路

使用 `source_environment=test` 的受控数据，在真实 RDS 验证以下四类：

- 隐患
- 巡检
- 作业票
- 培训考试

验证结果：

- 原始事件：4 条，全部 `processed`
- 结构化业务记录：4 条
- 专属业务表：四类各 1 条
- 附件：四类各 1 个
- 企业映射：命中平乡项目有效映射
- 时间：四类 `received_at` 与 `created_at` 偏差均为 0
- 去重：四类重复提交均被拦截
- 重放：隐患事件重放成功，业务记录仍为 1 条
- 统计：受保护聚合接口包含四类记录
- 列表与详情：四类记录均返回详情字段、附件和时间线

测试标记：`p1-four-types-1785029337096`

最终数据库对账：

- 原始事件：208
- 测试事件：208
- 真实环境事件：0
- 已处理事件：88
- 隔离事件：120
- 失败事件：0
- 业务记录：
  - 隐患 12
  - 巡检 74
  - 作业票 1
  - 培训 1
- 附件：4

一次 Windows `curl` 引号错误产生了 1 条无业务记录的隔离测试事件。该事件元数据已保存到受保护审计文件后定点删除，最终对账已恢复到上述结果。

## 六、账号与运行权限

### `anxun_codex`

- 类型：普通账号
- 精确权限：`SELECT, INSERT, UPDATE, DELETE`
- 已移除 `CREATE, ALTER, DROP`、账号管理、事件和触发器等权限。
- 现有迁移版本的只读检查仍可正常执行。

### `anxun_runtime`

- 类型：普通账号
- 精确权限：`SELECT, INSERT, UPDATE`
- 不具备持久表 `CREATE` 权限。
- 不具备 `ALTER`、`DROP`、`DELETE` 和账号管理权限。
- PM2 在线进程实际使用账号：`anxun_runtime`。

### 运行状态

- PM2 进程：`anxun-caoliao-webhook`
- 状态：`online`
- 数据源：`demo`
- 公共平乡页不会读取真实 RDS 数据。

## 七、RDS 安全配置

- 实例：`rm-2zek5t43u213g1p4p`
- 地域：北京
- 网络：VPC 内网
- 公网地址：无
- 白名单：仅 ECS 私网地址 `172.20.19.247`
- 自动备份：每日
- 数据备份保留：14 天
- 日志备份：启用，保留 14 天
- 运行环境文件权限：`600`
- 维护和紧急账号环境文件权限：`600`
- `.logs` 与 `.data` 目录权限：`700`
- Webhook 日志与 JSONL 文件权限：`600`
- 仓库未提交 RDS 密码、AccessKey、私钥或生产环境文件。

### 删除保护限制

当前 RDS 为订阅制 `Prepaid` 实例。调用
`ModifyDBInstanceDeletionProtection` 返回
`OperationDenied.PayType`，实例字段仍为 `DeletionProtection=false`。

阿里云 RDS MySQL 官方限制为：释放保护只支持按量付费或 Serverless 实例。若严格要求该字段为 `true`，必须先进行计费方式或实例迁移决策；本轮未擅自变更计费方式。

## 八、实际回退演练

在不切换正式站点、不修改生产数据库的条件下完成了真实恢复演练：

- 从 `webhook-app.tar.gz` 解压旧服务到临时目录。
- 在临时端口 `8792` 启动旧 Webhook，真实 POST 返回 `200` 和 `received`。
- 从 `www-html.tar.gz` 解压旧静态站点。
- 在临时端口 `8793` 启动，首页返回 `200`。
- 创建临时数据库 `anxun_p1_rollback_drill_20260726`。
- 在临时库完整执行 `001` 至 `004`。
- 实际执行 `004` down 回退。
- 验证 `004` 迁移记录和 `raw_body` 字段均从 1 变为 0。
- 删除临时数据库和临时文件。
- 正式 PM2 进程 PID 在演练前后保持不变。

第一次演练命令因备份包目录层级判断错误而失败，未切换或停止正式进程。修正目录后第二次演练成功。

## 九、线上回归

以下地址均返回 `200`：

- `https://axsztech.cn/`
- `https://www.axsztech.cn/`
- `https://axsztech.cn/platform/`
- `https://axsztech.cn/platform/tasks`
- `https://axsztech.cn/platform/hazards`
- `https://axsztech.cn/platform/records`
- `https://axsztech.cn/gov/pingxiang`
- `https://axsztech.cn/gov/pingxiang/companies`

接口边界：

- `POST /api/caoliao/webhook`：返回 `200`，受控重复数据未新增记录。
- 公网匿名访问 `GET /api/gov/pingxiang/dashboard`：返回 `401`。
- 公共平乡页面显示“演示环境”和“内部演示数据”。

浏览器检查：

- 官网：控制台 0 错误、0 警告。
- 平乡演示页：控制台 0 错误、0 警告。
- 中台登录页可访问；存在历史遗留的 `/favicon.ico` 404，不影响功能，且不是本轮 P1 改动引入。

## 十、敏感信息检查

- 未发现 RDS 密码、AccessKey、私钥或生产连接串进入本轮 Git 改动。
- 仓库中已有前端测试账号配置，属于 P2 正式鉴权替换范围；当前真实数据接口不依赖该前端测试登录，也不能凭该登录匿名读取真实 RDS。
- Webhook 仍按现有草料兼容模式接收请求，本轮未擅自启用会中断草料推送的认证开关。

## 十一、结论

P1 的代码、部署、004 迁移、历史时间修复、四类真实 RDS 受控链路、账号最小权限、备份、回退演练和线上回归均已完成。

严格按任务书字面标准，当前不能标记为 100%，唯一未满足项是订阅制 RDS 不支持将 `DeletionProtection` 设置为 `true`。该限制已由阿里云 API 实际返回和产品规则共同确认。除非接受订阅制实例的现有保护边界，或另行批准计费方式/实例迁移，否则不应虚报该项完成。

在该决策前，不启动 P2 登录鉴权开发。
