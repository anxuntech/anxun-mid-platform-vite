# AGENTS.md

- 优先少改动、可运行、可验证。
- 不把“代码已写”误报成“线上已正式可用”。

## 当前授权任务：P3 AI 数据助手与多县域配置化

本轮执行《P3｜AI 数据助手、多县域与政府平台接口任务书》的已授权部分：

- 基于 P1 MySQL 数据底座和 P2 正式认证、项目授权体系建设 AI 数据助手。
- DeepSeek 只负责将问题解析为严格受控的 Intent 和组织摘要，不得直接连接数据库或生成、执行任意 SQL。
- 后端必须重新校验当前账号、项目、县域、企业、时间范围、字段和最大结果数。
- 首轮只对安巡管理员开放 AI 助手，使用明确标记为 `test` 的数据验证。
- AI 测试结果必须标注测试环境，不得进入平乡正式页面或冒充真实企业运行数据。
- 实现调用上限、Token 上限、超时、审计、提示注入防护、功能开关和模型不可用降级。
- 将平乡硬编码的页面、API 和聚合入口收口为同一套多县域配置机制，保持现有 `/gov/pingxiang` 路由兼容。
- 使用第二个纯演示县域验证路由、权限和数据完全隔离，不复制第二套页面代码。
- 完成构建、TypeScript、服务端安全测试、浏览器自动化、部署和回退验证。

## 用户确认的阶段调整

- 跳过 P1 连续运行七天的等待条件。
- P3 首轮允许使用现有 `test` 数据验证 DeepSeek。
- 后续真实链路验收记录必须可识别、可对账、可按批次安全清理，不得宽范围删除。
- 政府第三方平台接口当前无需求，本轮不开发、不联调、不生成虚假验收结论。
- DeepSeek API Key 只允许存放在服务器受保护环境变量，不得写入 Git、前端、测试快照、日志或报告。

## 本轮明确禁止

- 修改 P1 已验收的四类 Webhook 识别、去重、入库和时间处理逻辑。
- 削弱 P2 登录、会话、项目范围、下载权限、审计和匿名接口保护。
- 向应急局、保险账号或公共页面开放 `test` 数据。
- 让 AI 直接持有数据库账号、表结构管理权限或任意 SQL 工具。
- 让模型输出执法结论、风险定级、处罚建议或事故责任认定。
- 自动向任何政府或第三方平台推送数据。
- 修改官网、自助部署系统或无关中台页面。
- 强制推送、覆盖远程 `main`，或在旧脏工作区继续开发。
- 在文档、日志、Git 或聊天回报中回显密码、AccessKey、DeepSeek Key、Token 或完整连接串。

除本章节明确授权的范围外，原有安全边界继续有效。
