# AGENTS.md

- 优先少改动、可运行、可验证。
- 不把“代码已写”误报成“线上已正式可用”。

## 当前授权任务：P2 正式账号、接口保护与真实数据运行

本轮执行《P2｜正式账号、接口保护与真实数据运行任务书》：

- 建立 `admin` 与 `project_viewer` 两类正式后端账号。
- 建立 bcrypt 密码哈希、服务器会话、退出、禁用和登录审计。
- 建立用户项目绑定、县域/项目/企业后端数据范围控制。
- 保护政府端真实数据、详情与下载接口。
- 建立汇总下载、明细下载权限和下载审计。
- 删除前端明文账号与 `localStorage` 可信会话。
- 分离平乡公开演示入口与登录后真实数据入口。
- 完成越权、安全、构建、自动化回归、上线和回退验证。
- 基于 P1 已验收的 MySQL 数据底座、迁移体系和 `anxun_runtime` 运行账号设计方案。
- 在认证与越权验收完成前保持公共页面使用演示数据，真实数据接口继续拒绝匿名访问。

## 本轮明确禁止

- 修改 P1 已验收的四类业务数据链路、Webhook 识别与入库逻辑。
- 修改官网、中台、平乡 V2.1 页面视觉和业务交互。
- 向公网匿名开放真实数据，或让真实接口失败时回填演示数据。
- 修改 Nginx 总体架构、服务器安全策略或自助部署系统。
- 强制推送或覆盖远程 `main`。
- 在文档、日志或 Git 中写入密码、AccessKey、Token、密钥或完整连接串。
- 扩展到企业登录端、复杂 RBAC/ABAC、SSO、MFA、短信、AI 或跨县域 UI。

除本章节明确授权的范围外，原有安全边界继续有效。
