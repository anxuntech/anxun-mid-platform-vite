# AGENTS.md

- 优先少改动、可运行、可验证。
- 不把“代码已写”误报成“线上已正式可用”。

## 当前授权任务：P1｜RDS MySQL正式数据底座与生产数据链路建设

本轮授权覆盖此前与数据库、真实数据链路和草料 Webhook 入库相关的限制，但必须保护现有官网、草料自动部署、后端公开路径、平乡政府端 V2.1 和部署体系。

本阶段允许：

- 初始化阿里云 RDS MySQL，创建业务数据库和最小权限普通账号。
- 配置 RDS 白名单、备份、内网连接和 ECS 受限环境文件。
- 建立数据库表、索引、迁移、回退、对账和恢复脚本。
- 备份并迁移历史 JSONL，历史数据统一标记为 `test`。
- 在保留现有公开路径和表单识别能力的前提下，改造草料 Webhook 原始事件入库和四类业务结构化解析。
- 建设 MySQL 数据去重、异常隔离、失败重放和受保护的政府端真实数据聚合接口。
- 建立演示数据与 MySQL 共用的数据契约和统一数据访问层。
- 修改完成 P1 所需的后端、配置、测试、脚本和部署文件。
- 安装完成 P1 所需的阿里云官方 CLI、SDK、MySQL 客户端、Node.js 数据库驱动、迁移依赖和测试工具。
- 在不改变 P0/V2.1 视觉和交互的情况下完成真实数据服务接入验证。

## 本轮明确禁止

- 强制推送或覆盖远程 main。
- 修改 `axsztech.cn` 和 `www.axsztech.cn` 官网内容、二级页面或原中台视觉与交互。
- 重做或降低平乡政府端 P0/V2.1 页面视觉、交互、宽抽屉和三级详情能力。
- 修改草料表单结构、草料自动化部署逻辑或现有企业配置。
- 删除、截断、覆盖现有 JSONL，或把测试、演示、历史联调数据标记为真实数据。
- 在 P2 后端认证完成前公开真实企业数据接口。
- 开发 P2 正式账号认证、权限系统、下载审计或企业账号。
- 开发 P3 AI 助手、DeepSeek 接入或政府第三方平台接口。
- 申请或开启 RDS 公网连接地址。
- 将密码、AccessKey、Token、密钥或数据库连接串写入 Git、前端包、普通日志、截图或报告。
- 修改 Nginx 总体架构、服务器安全策略或自助部署系统。
- 删除 RDS 实例、删除业务数据库、删除原始事件或执行不可恢复操作。
- 扩展本任务之外的 UI、业务功能或县域页面。
- 将演示环境误标记为正式环境，或让公共 `/gov/pingxiang` 默认读取真实数据。

## P1 数据环境约束

- 公共平乡政府端默认保持 `PINGXIANG_DATA_SOURCE=demo`。
- 历史 JSONL 和未显式启用的来源连接器默认使用 `source_environment=test`。
- 只有经过明确启用和企业映射的来源连接器才允许写入 `source_environment=real`。
- P2 认证上线前，MySQL 真实数据接口仅允许 ECS 本机、自动化测试或内部密钥访问。
- MySQL 写入、真实数据读取和 Webhook 强制认证必须分别使用独立配置开关。
- 关闭 MySQL 相关开关后，官网、中台、草料自动部署和 P0/V2.1 演示页面行为必须与当前线上一致。

除本章节明确授权的范围外，原 AGENTS.md 其余约束继续有效。
