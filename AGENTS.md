# AGENTS.md

- 优先少改动、可运行、可验证。
- 不把“代码已写”误报成“线上已正式可用”。

## 当前授权任务：P2 第一阶段

本轮仅执行《P2｜正式账号、接口保护与真实数据运行任务书》的第一阶段：

- 审计现有前端登录、后端接口保护、数据范围与下载能力。
- 设计最小正式认证、服务器会话、角色与项目数据范围架构。
- 编制后续实施、测试、上线和回退计划。
- 基于 P1 已验收的 MySQL 数据底座、迁移体系和 `anxun_runtime` 运行账号设计方案。
- 保持公开 `/gov/pingxiang` 使用演示数据，真实数据接口继续拒绝匿名访问。

## 本轮明确禁止

- 实施登录、会话、RBAC、账号种子或数据库迁移。
- 修改 P1 已验收的四类业务数据链路、Webhook 识别与入库逻辑。
- 修改官网、中台、平乡 V2.1 页面视觉和业务交互。
- 向公网匿名开放真实数据，或让真实接口失败时回填演示数据。
- 修改 Nginx、部署体系、服务器安全策略或自助部署系统。
- 强制推送、覆盖远程 `main`、合并到 `main` 或执行线上部署。
- 在文档、日志或 Git 中写入密码、AccessKey、Token、密钥或完整连接串。
- 扩展到企业登录端、复杂 RBAC/ABAC、SSO、MFA、短信、AI 或跨县域 UI。

除本章节明确授权的范围外，原有安全边界继续有效。
